#include<iostream>
#include<lexer.h>
#include<string>
#include<utils.h>
int main(){
    using namespace dy;
    // dy_error(FATAL_ERROR,"dayyun");
    // std::string s;
    // dy::Scanner a;
    // while(std::getline(std::cin,s))
    // {
        
    //     a.set_content(s);
    //     a.scan();
    //     a.print_tokens();
    //     std::cout<<std::endl;
        
    // }
    return 0;
}